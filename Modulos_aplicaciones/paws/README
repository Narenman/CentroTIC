Esta carpeta contiene los archivos correspondientes al proyecto TVWS

Para ejecutar el proyecto debe tener instalado:

1) sudo apt-get install python-pip
2) sudo pip install paho-mqtt
3) gnuradio-companion

* todos los .csv corresponden a los datos con los que se llena la 
base de datos
* database psql.txt contiene las instrucciones para llenar la base de datos
por la linea de comandos
* VistaUDP.py, permite visualizar la señal enviada atraves de UDP
* TVWS.py contiene una simulacion de la transmision de una señal de radio, 
es decir el codigo que se ejecutará en el sistema embebido del esclavo
* Slave.py es el script que se ejecuta en el dispositivo esclavo
* Master.py es el script que se ejecuta en el dispositivo maestro

Orden de ejecucion:
1) Master.py
2) VistaUDP.py 
3) Slave.py